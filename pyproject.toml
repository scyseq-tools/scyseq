[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "scyseq"
version = "0.1.0"
description = "Provides objects and tools to analyze symbolic sequences"
readme = "README.md"
authors = [
    { name = "Laurent Pezard", email = "laurent.pezard@univ-amu.fr" },
    { name = "Jean-Luc Blanc", email = "jean-luc.blanc@univ-amu.fr" }
]
license = { text = "MIT" }
requires-python = ">=3.8"
dependencies = ["numpy", "matplotlib", "scipy"]

[project.optional-dependencies]
dev = ["pytest"]
docs = ["sphinx", "sphinx-gallery", "nbsphinx", "sphinxcontrib-bibtex"]

[tool.hatch.build.targets.wheel]
packages = ["src/scyseq"]

[tool.hatch.envs.default]
features = ["dev", "docs"]
